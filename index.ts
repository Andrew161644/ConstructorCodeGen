import { INPUT_PATH, OUTPUT_PATH } from "./paths";
import { buildComponents } from "./scenarios";

const inputPath = INPUT_PATH;
const outputPath = OUTPUT_PATH;

buildComponents(inputPath, outputPath);
